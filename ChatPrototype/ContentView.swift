//
//  ContentView.swift
//  ChatPrototype
//
//  Created by Will Krause on 4/13/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hey there, universe!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
